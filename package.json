{
  "name": "reversiweb",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:ipad": "playwright test --project=ipad",
    "test:flip-order": "playwright test --project=flip-order"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "playwright": "^1.50.0"
  }
}
